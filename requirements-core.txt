# Runtime deps for core/scanner.py + core/yf_ratelimit.py -- shared by
# both the Streamlit app (requirements.txt already lists these too) and
# the FastAPI/Celery backend (requirements-server.txt references this
# file directly, via `-r requirements-core.txt`, so the backend Docker
# image doesn't have to install streamlit/plotly/scipy just to get
# pandas/numpy/yfinance).
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
requests>=2.31.0
curl_cffi>=0.6.2
